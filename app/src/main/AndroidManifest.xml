<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HookTrans">

        <!-- LSPosed / Xposed module descriptor -->
        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="@string/module_description" />
        <meta-data android:name="xposedminversion" android:value="93" />
        <meta-data android:name="xposedscope" android:resource="@array/xposed_scope" />
        <!--
            Opts into LSPosed's managed preferences. Without this the framework does not
            intercept MODE_WORLD_READABLE, the file stays inside /data/data where no hooked
            process can traverse to it, and every hooked app silently falls back to
            HookConfig() defaults — which looks like "the coverage toggles do nothing".
        -->
        <meta-data android:name="xposedsharedprefs" android:value="true" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AppPickerActivity"
            android:exported="false"
            android:label="@string/title_app_scope" />

        <!--
            Exported so hooked processes can bind to it: they run under their own uid, so no
            manifest permission can express "the apps I was injected into". Access is gated in
            code instead — TranslatorService resolves the caller from its binder uid (never
            from a caller-supplied package name), refuses translation for packages outside the
            configured scope, strips credentials from the config it hands back, and restricts
            the maintenance calls to the module's own uid.
        -->
        <service
            android:name=".service.TranslatorService"
            android:exported="true"
            android:foregroundServiceType="specialUse"
            android:process=":engine">
            <intent-filter>
                <action android:name="io.hooktrans.BIND_TRANSLATOR" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Serves on-device translation requests to hooked app processes." />
        </service>

        <service
            android:name=".access.ScreenTranslateService"
            android:exported="true"
            android:label="@string/accessibility_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/screen_translate_service" />
        </service>
    </application>
</manifest>
